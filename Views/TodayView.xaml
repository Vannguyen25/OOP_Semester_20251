<UserControl x:Class="OOP_Semester.Views.TodayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BgLightBrush" Color="#F7F9FC"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextMainBrush" Color="#2D3748"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#718096"/>
        <SolidColorBrush x:Key="BorderSoftBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#F58634"/>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar" x:Key="FlatProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Background" Value="#E5E5E5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                            <Rectangle x:Name="PART_Track" />
                            <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left">
                                <Grid x:Name="Foreground">
                                    <Border x:Name="Indicator" Background="{TemplateBinding Foreground}" CornerRadius="5"/>
                                </Grid>
                            </Decorator>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">

                <TextBlock Text="Today's Focus" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource TextMainBrush}"/>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="1.2*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="25">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="{Binding Greeting}" FontSize="22" FontWeight="Black" Foreground="{StaticResource TextMainBrush}"/>
                                <TextBlock Text="{Binding StreakMessage}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,20"/>

                                <StackPanel Orientation="Horizontal">
                                    <Grid Margin="0,0,30,0">
                                        <Ellipse Width="45" Height="45" Stroke="{StaticResource AccentOrangeBrush}" StrokeThickness="4"/>
                                        <TextBlock Text="{Binding DailyProgressPercent, StringFormat={}{0}%}" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                    </Grid>

                                    <StackPanel VerticalAlignment="Center" Margin="0,0,40,0">
                                        <TextBlock Text="Daily Goal" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" />
                                        <TextBlock Text="Keep going!" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                                    </StackPanel>

                                    <Rectangle Width="1" Fill="#EEE" Height="30" VerticalAlignment="Center"/>

                                    <StackPanel VerticalAlignment="Center" Margin="30,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="💰" FontSize="16" Margin="0,0,5,0"/>
                                            <StackPanel>
                                                <TextBlock Text="COINS" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding Coins, StringFormat={}{0} Earned}" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="{StaticResource AccentOrangeBrush}" CornerRadius="24" Padding="20">
                        <StackPanel>
                            <Border Background="#33FFFFFF" HorizontalAlignment="Left" CornerRadius="10" Padding="8,2">
                                <TextBlock Text="Active Challenge" Foreground="White" FontSize="10" FontWeight="Bold"/>
                            </Border>

                            <TextBlock Text="{Binding ChallengeName}" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,15,0,0"/>
                            <TextBlock Text="{Binding ChallengeProgressText}" Foreground="#FFE0B2" FontSize="12" Margin="0,2,0,15"/>

                            <Grid Height="8">
                                <Border Background="#4D000000" CornerRadius="4"/>
                                <Border Background="White" CornerRadius="4" HorizontalAlignment="Left" 
                                        Width="{Binding ChallengePercent}"/>
                            </Grid>

                            <Button Content="Continue" Margin="0,15,0,0" Background="White" Foreground="{StaticResource AccentOrangeBrush}" 
                                    BorderThickness="0" FontWeight="Bold" Padding="10,6" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="☀️" FontSize="18" Margin="0,0,8,0"/>
                        <TextBlock Text="Daily Routine" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                    </StackPanel>
                </Grid>

                <ItemsControl ItemsSource="{Binding Habits}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="16" Margin="0,0,0,12" Padding="15" 
                                    BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" Grid.ColumnSpan="2"
                                            Background="Transparent" BorderThickness="0" Padding="0"
                                            HorizontalContentAlignment="Stretch" Cursor="Hand"
                                            Command="{Binding DataContext.OpenHabitMonthCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">

                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Button.Template>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" Margin="0,0,15,0">
                                                <Border.Background>
                                                    <SolidColorBrush Color="{Binding ColorHex}"/>
                                                </Border.Background>
                                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe UI Emoji" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextMainBrush}"/>
                                                <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>

                                                <StackPanel Orientation="Horizontal" Visibility="{Binding HasStatus, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Button>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                                        <Button Content="+1" Width="34" Height="34" Margin="0,0,6,0"
                                                Background="{StaticResource BgLightBrush}" BorderThickness="0" FontWeight="Bold" Cursor="Hand"
                                                Command="{Binding DataContext.AddOneCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsCounterType, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="10"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="✓" Width="34" Height="34" 
                                                Background="{StaticResource PrimaryBrush}" BorderThickness="0" 
                                                Foreground="White" FontWeight="Black" Cursor="Hand"
                                                Command="{Binding DataContext.CompleteAllCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="12"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="⏭" Width="34" Height="34" Margin="10,0,0,0"
                                                Background="{StaticResource BgLightBrush}" BorderThickness="0" Cursor="Hand"
                                                Command="{Binding DataContext.SkipHabitCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="10"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="🗑" Width="34" Height="34" Margin="8,0,0,0"
                                                Background="{StaticResource BgLightBrush}" BorderThickness="0" Cursor="Hand"
                                                Command="{Binding DataContext.DeleteHabitCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="10"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>

            <StackPanel Grid.Column="2">

                <Border Style="{StaticResource CardBorderStyle}" Padding="18">
                    <StackPanel>
                        <Border Background="#333B82F6" CornerRadius="999" BorderBrush="{StaticResource BorderSoftBrush}" 
                                BorderThickness="1" Padding="10,4" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding CurrentPetLevelStatus}" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" FontSize="10"/>
                        </Border>

                        <Image Source="{Binding CurrentPetImage}" Height="120" Margin="0,10,0,0" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="5"/>
                            </Image.Effect>
                        </Image>

                        <TextBlock Text="{Binding CurrentPet.Name}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>

                        <Border Background="{StaticResource BgLightBrush}" CornerRadius="16" Padding="12" Margin="0,14,0,0">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,6">
                                    <TextBlock Text="Hunger" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding HungerPercent, StringFormat={}{0}%}" DockPanel.Dock="Right" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Height="14" Width="182"/>
                                </DockPanel>
                                <ProgressBar Value="{Binding HungerPercent}" Maximum="100" Style="{StaticResource FlatProgressBar}" Foreground="{Binding AccentOrangeBrush}" Margin="0,0,0,14"/>

                                <DockPanel Margin="0,0,0,6">
                                    <TextBlock Text="Happiness" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Width="66"/>
                                    <TextBlock Text="{Binding HappinessPercent, StringFormat={}{0}%}" DockPanel.Dock="Right" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Width="182" Height="13"/>
                                </DockPanel>
                                <ProgressBar Value="{Binding HappinessPercent}" Maximum="100" Style="{StaticResource FlatProgressBar}" Foreground="{StaticResource AccentOrangeBrush}" Margin="0,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Padding="18" Margin="0,16,0,0">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,12">
                            <TextBlock Text="{Binding WeekTitle}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextMainBrush}"/>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="‹" Width="30" Height="30" Background="{StaticResource SurfaceBrush}" BorderThickness="1" BorderBrush="{StaticResource BorderSoftBrush}" Cursor="Hand"
                                        Command="{Binding PrevWeekCommand}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="999"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Content="›" Width="30" Height="30" Margin="8,0,0,0" Background="{StaticResource SurfaceBrush}" BorderThickness="1" BorderBrush="{StaticResource BorderSoftBrush}" Cursor="Hand"
                                        Command="{Binding NextWeekCommand}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="999"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </DockPanel>

                        <ItemsControl ItemsSource="{Binding WeekDays}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand"
                                            Command="{Binding DataContext.SelectDateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding DayLabel}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                            <Border Width="34" Height="34" Margin="0,6,0,0" CornerRadius="999" BorderThickness="1" BorderBrush="{StaticResource BorderSoftBrush}" Background="{StaticResource SurfaceBrush}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding DateLabel}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>