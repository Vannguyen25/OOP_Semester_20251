<UserControl x:Class="OOP_Semester.Views.AuthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             FontFamily="Segoe UI" FontSize="14">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#f58634"/>
        <SolidColorBrush x:Key="BgLight" Color="#f8f8f5"/>
        <SolidColorBrush x:Key="BorderColor" Color="#e9e8ce"/>

        <Style TargetType="Control" x:Key="InputBoxStyle">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="15,0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="28">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,3" Padding="5,10">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgLight}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#FFFBEB">
            <Ellipse Width="150" Height="150" Fill="#f58634" Opacity="0.3" Margin="50,50,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                <Ellipse.Effect>
                    <BlurEffect Radius="40"/>
                </Ellipse.Effect>
            </Ellipse>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <Image Source="/Images/System/Main_Pic_Login.png" 
                       Width="400" Margin="0,0,0,30" 
                       Stretch="Uniform"/>

                <TextBlock Text="Nuôi dưỡng thói quen," FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Chăm sóc thú cưng" FontSize="32" FontWeight="Bold" Foreground="#f58634" HorizontalAlignment="Center"/>
                <TextBlock Text="Mỗi thói quen tốt bạn xây dựng sẽ giúp" FontSize="16" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                <TextBlock Text="thú cưng lớn lên khỏe mạnh mỗi ngày." FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <StackPanel VerticalAlignment="Center" Margin="80,0" MaxWidth="450">

                <StackPanel Orientation="Horizontal" Margin="0,0,0,30" Height="48" Width="341">
                    <Image Source="/Images/System/Logo.png" Width="40" Height="40" Margin="0,0,10,0"/>
                    <TextBlock Text="HabitPet" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">

                    <Button Content="Đăng nhập" Command="{Binding LoginTabCommand}">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource TabBtn}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRegisterMode}" Value="False">
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsRegisterMode}" Value="True">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Đăng ký" Command="{Binding RegisterTabCommand}">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource TabBtn}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRegisterMode}" Value="True">
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsRegisterMode}" Value="False">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <TextBlock Text="{Binding HeaderTitle}" FontSize="24" FontWeight="Bold" Margin="0,0,0,5"/>

                <TextBlock Text="Tên đăng nhập" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputBoxStyle}"/>

                <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <PasswordBox x:Name="txtPassword" 
                             Style="{StaticResource InputBoxStyle}" Margin="0,0,0,20"/>

                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRegisterMode}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="Nhập lại mật khẩu" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="txtConfirmPassword" 
                                 Style="{StaticResource InputBoxStyle}" Margin="0,0,0,20"/>
                </StackPanel>

                <Button Content="{Binding SubmitButtonText}" 
                        Style="{StaticResource PrimaryBtn}"
                        Click="BtnSubmit_Click" Height="50" Width="176"/>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>