<UserControl x:Class="OOP_Semester.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OOP_Semester.ViewModels"
             xmlns:v="clr-namespace:OOP_Semester.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:TodayViewModel}">
            <v:TodayView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HabitViewModel}">
            <v:HabitView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingViewModel}">
            <v:SettingView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FeedingViewModel}">
            <v:FeedingView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ShopViewModel}">
            <v:ShopView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:NotifyViewModel}">
            <v:NotifyView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatisticViewModel}">
            <v:StatisticView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChallengeViewModel}">
            <v:ChallengeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AdminViewModel}">
            <v:AdminView />
        </DataTemplate>

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="MenuButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#757564"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="MainMenuScope"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" >
                                <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                           FontFamily="Segoe UI Symbol" FontSize="18" Margin="0,0,10,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#FFF8E1"/>
                                <Setter Property="Foreground" Value="#f58634"/>
                            </MultiTrigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#f58634"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#f8f8f5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" x:Name="MenuColumn"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#e9e8ce" BorderThickness="0,0,1,0">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,40">
                    <Border Width="48" Height="48" CornerRadius="24" BorderBrush="#f58634" BorderThickness="2">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding AvatarUrl}" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock Text="{Binding Username}" FontWeight="Bold" FontSize="16" Foreground="#1c1c0d"/>
                        <TextBlock Text="Welcome back!" FontSize="12" Foreground="#f58634"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <RadioButton Content="Quản trị" Tag="🛡️" Command="{Binding NavigateCommand}" CommandParameter="Administrator"
                                 Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Administrator">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Hôm nay" Tag="📅" Command="{Binding NavigateCommand}" CommandParameter="Today">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Today">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Thói quen" Tag="📝" Command="{Binding NavigateCommand}" CommandParameter="Habits">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Habits">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Thử thách" Tag="🏆" Command="{Binding NavigateCommand}" CommandParameter="Challenges">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Challenges">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Thú cưng" Tag="🐶" Command="{Binding NavigateCommand}" CommandParameter="Pet">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Pet">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Cửa hàng" Tag="🛍️" Command="{Binding NavigateCommand}" CommandParameter="Shop">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Shop">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Thống kê" Tag="📊" Command="{Binding NavigateCommand}" CommandParameter="Statistics">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Statistics">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton Content="Thông báo" Tag="🔔" Command="{Binding NavigateCommand}" CommandParameter="Notify">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Notify">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <RadioButton Content="Cài đặt" Tag="⚙️" Command="{Binding NavigateCommand}" CommandParameter="Settings">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActiveTab}" Value="Settings">
                                        <Setter Property="IsChecked" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="#f8f8f5" Margin="0,0,-75,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ToggleButton x:Name="MenuToggleButton" Grid.Row="0" Width="30" Height="30" HorizontalAlignment="Left" Margin="20,5"
                          Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MenuToggleButton_Click">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <Path Data="M0,6 H20 M0,12 H20 M0,18 H20" Stroke="#1c1c0d" StrokeThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="15"/>

        </Grid>
    </Grid>
</UserControl>